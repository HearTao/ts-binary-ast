export enum Magic {
    Header = "BINJS",
    Version = 1
}

export enum Section {
    Grammar = "[GRAMMAR]",
    Strings = "[STRINGS]",
    Tree = "[TREE]"
}

export enum Compression {
    Identity = "identity;"
}